---
title: P-dit Probabilistic Ising Machine for Solving the Quadratic Assignment Problem
title_zh: 用于解决二次分配问题的P-dit概率伊辛机
authors: "Christian Duffee, Chadbourne M. Burling-Smith, Jordan Athas, Andrea Grimaldi, Giovanni Finocchio, Ermin Wei, Pedram Khalili Amiri"
date: 2026-05-23
pdf: "https://arxiv.org/pdf/2605.24408v1"
tags: ["query:aed-drone"]
score: 8.0
evidence: 二次分配问题解决最优设施布局
tldr: "二次分配问题(QAP)是典型组合优化难题，传统求解器难以高效找到最优解。本文提出基于概率d维变量(p-dit)的伊辛机(PIM)，每个p-dit代表一个位置，随机选择设施分配。实验表明，在QAP库中PIM在95%实例上找到已知最优解，Gurobi仅36%；最大问题上求解速度提升2-3个数量级。GPU并行实现进一步超越现有算法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24408v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1659, \"height\": 1583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24408v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1711, \"height\": 1420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24408v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1352, \"height\": 878, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24408v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1006, \"height\": 1591, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24408v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1656, \"height\": 1136, \"label\": \"Table\"}]"
motivation: 解决QAP的经典精确求解器在大规模实例上效率低下，需要更高效的近似方法。
method: 将每个位置建模为多状态p-dit，随机翻转设施分配，通过局部更新机制模拟概率伊辛模型。
result: "在QAP Library数据集上，PIM在95%实例达到最优解，Gurobi仅36%；最大实例求解速度提升2-3个数量级。"
conclusion: P-dit概率伊辛机为QAP提供高效并行求解方案，在广泛实例上显著优于传统求解器。
---

## 摘要
组合优化问题代表了广泛的现实场景，其中复杂的相互作用使得难以找到最佳解决方案。一个例子是二次分配问题（QAP），它涉及在固定位置确定设施的最佳布局，以最小化物料流与设施距离的乘积。这种表示描述了许多现实场景，包括供应链的总运输成本。在这项工作中，使用概率d维变量（p-dits）实现了一种概率伊辛机（PIM）方法，这些变量是概率比特（p-bits）的泛化、多状态和多维扩展。每个p-dit对应一个位置，并根据其他p-dit的影响在设施分配之间随机振荡。我们表明，在相同的运行时间和CPU下，PIM在QAP库数据集的95%的考虑实例上找到了已知最佳解，而标准Gurobi求解器仅为36%。对于库中唯一的最大问题，观察到达到特定解质量所需的时间减少了2到3个数量级。我们还通过GPU实现展示了PIM的并行化。与最先进的QAP求解器算法相比，无论是CPU还是GPU实现的p-dit伊辛机都始终优于它们。

## Abstract
Combinatorial optimization problems represent a wide range of real-world scenarios where complicated interactions make it difficult to find the best solution. One example is the quadratic assignment problem (QAP), which involves determining the optimal placement of facilities at set locations which minimizes the products of material flow and facility distance. This representation is descriptive of many real-world scenarios, including the aggregate transportation costs of a supply chain. In this work, a probabilistic Ising machine (PIM) approach is implemented using probabilistic d-dimensional variables (p-dits), which are generalized, multi-state and multi-dimensional extensions to probabilistic bits (p-bits). Each p-dit corresponds to a location and stochastically oscillates between facility assignments based on the influence of the other p-dits. We show that with the same runtime and CPU, the PIM finds the best-known solution on 95% of considered instances from the QAP Library dataset, compared to just 36% for the standard Gurobi solver. For the unique largest problem in the library, a 2 to 3 order-of-magnitude decrease is observed in the time needed to reach specific solution qualities. We also show parallelization of our PIM through GPU implementations. A comparison to state-of-the-art QAP solver algorithms shows that they are consistently outperformed by both CPU and GPU implementations of the p-dit Ising machine.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究方向**：组合优化中的**二次分配问题（QAP）**，要求在固定位置间分配设施，使物料流与距离乘积之和最小。
- **重要性**：QAP 是 NP-hard 问题，广泛应用于供应链、仓库管理、过程通信等领域，大规模实例（n<50 已难求解）对传统精确求解器构成巨大挑战。
- **现有局限**：标准商业求解器（如 Gurobi）在大规模 QAP 实例上效率低，常无法在合理时间内找到最优解；而专用启发式算法往往需要调整特定参数或依赖昂贵硬件。
- **动机**：探索基于**概率伊辛机（PIM）** 的通用、高效且可并行的近似求解方法，以弥补传统方法的不足。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将 QAP 映射为**概率伊辛模型**，每个位置用一个**概率 d 维变量（p-dit）** 表示，其状态对应分配的设施编号；p-dit 根据其他 p-dit 的“影响力”随机切换状态，最终收敛到低能量（低代价）配置。
- **关键技术细节**：
  - **变量表示**：每个 p-dit 是一个 N 维单位向量，N 为设施数。初始时每个 p-dit 分配唯一设施（保证可行解）。
  - **影响力计算**：对位置 i 和设施 a，影响力 \(I_i^a = -\sum_j (D_{ij} F_{a,p(j)} + D_{ji} F_{p(j),a})\)，其中 D 为距离矩阵，F 为流量矩阵，p(j) 为位置 j 当前分配的设施。
  - **更新机制**：每次迭代每个 p-dit 尝试与另一个 p-dit 交换设施（或保持原样），交换的能量变化 \(\Delta E\) 由两个 p-dit 的影响力差计算（对称距离下可简化）。交换概率采用 softmax 形式：\(P(\text{swap}) = e^{-\beta \Delta E} / \sum_k e^{-\beta \Delta E_k}\)。
  - **并行回火（parallel tempering）**：使用多个副本（10 个）运行在不同温度（\(\beta\)），定期交换配置，帮助跳出局部最优。还引入“超时强制交换”机制（100 次交换周期无变化则交换最冷和最热副本）。
  - **\(\beta\) 范围自动选择**：通过短时间扫描确定有效温度区间。
- **算法流程**：
  1. \(\beta\) 范围扫描（可选）。
  2. 主循环：每个副本独立运行，每次迭代对所有 p-dit 逐一计算交换概率并随机更新。
  3. 每 4 次迭代后执行副本间的交换（从低 \(\beta\) 到高 \(\beta\)，若能量更低则交换）。
  4. 重复直到达到停止条件（时间限制或找到最优解）。
- **GPU 并行实现**：
  - **GPU-PIM**：将副本和重复问题并行化至 GPU block 级别，更新中并行计算影响力变化。
  - **GPU-CUPIM**：进一步**并发更新**，即所有可能交换先计算快照能量差，再通过冲突解决（选择能量降低最大的交换）同时应用，牺牲一定收敛特性换取速度。

## 3. 实验设计

- **数据集**：**QAPLIB 标准库**（除特殊问题 tai256c 外共 127 个实例），涵盖从小型（n=20）到大型（n=256）问题。tai256c 因特殊对称性单独处理。
- **基准方法**：
  - **Gurobi 12.0.2**（商业 MILP 求解器），使用标准二元变量 + 约束的 QAP 建模，设置相同线程数、内存和时间限制。
  - 文献中的专用 QAP 算法：**CP TS**（并行禁忌搜索）、**PHA**（并行混合算法）、**PABC-QAP**（人工蜂群）、**BLS**（破坏性局部搜索）。
- **对比指标**：
  - 是否找到已知最优解（BKS）的比例。
  - 达到最优解的时间。
  - 平均百分比偏差（APD）：相对于已知最优解的偏差百分比。
- **主要实验组**：
  1. **CPU-PIM vs Gurobi**：127 个 QAPLIB 实例，20 线程，1000 秒限制（图 2）。
  2. **特大问题 tai256c**：10.7 小时运行，比较进化曲线（图 3）。
  3. **GPU 加速效果**：CPU-PIM、GPU-PIM、GPU-CUPIM 在 QAPLIB 子集上对比运行时间与解质量（图 4）。
  4. **与 4 种专用算法对比**：31 个代表性实例（表 1）。

## 4. 资源与算力

- **硬件平台**：
  - CPU：AMD Ryzen 9 7900X（12 核超线程）。
  - GPU：NVIDIA GeForce RTX 4070。
  - 内存：64 GB DDR5 4800MT/s。
- **算力配置**：
  - CPU-PIM：20 个独立线程，每个线程内 10 个副本。
  - GPU-PIM：32 个独立副本集，GPU 内核持续运行。
  - GPU-CUPIM：同样 32 个副本集，但更新机制不同。
- **限制**：Gurobi 设置内存上限 40 GB，时间上限 1000 秒（tai256c 除外）。PIM 的 \(\beta\) 扫描时间计入运行时间，Gurobi 的预求解时间计入。
- **注意**：论文未明确训练模型（无监督），所有计算为一次推理求解。

## 5. 实验数量与充分性

- **数量**：
  - 主对比覆盖 QAPLIB 全部 127 个实例（tai256c 单独处理）。
  - GPU 对比覆盖 14/16 个不同规模问题的子集（图 4 显示部分结果）。
  - 与专用算法对比 31 个常见实例（表 1）。
  - 每个实例仅一次运行（但 CPU-PIM 内有 20 个独立线程，相当于多次独立尝试）。
- **充分性与公平性**：
  - **优势**：使用了标准公开数据集 QAPLIB，对比方法（Gurobi、文献算法）均为公认基准。资源限制与 PIM 保持一致（线程数、内存、时间）。
  - **局限**：未进行多次重复实验（如多次启动不同随机种子），但多个独立线程已部分体现统计特性。GPU 对比中未控制完全相同迭代次数，且部分结果（图 4）只展示差异较大的问题，选择性可能引入偏差。
  - **公平性**：Gurobi 并非专用 QAP 求解器，但作为商业标准，对比具有参考价值。专用算法使用不同硬件和参数，比较结果（表 1）难以严格定量（硬件差异），但论文仍展示了明确的趋势。

## 6. 论文的主要结论与发现

- **CPU-PIM 显著优于 Gurobi**：
  - 在 95% 的实例（121/127）上找到已知最优解，而 Gurobi 仅 36%（46/127）。
  - 在两者均找到最优解的问题中，PIM 平均快 66 秒，81% 的问题上更快。
  - 唯一最优解未被找到的 6 个实例中，PIM 的解质量也更高。
- **特大问题 tai256c**：
  - PIM 在 **517 秒（8.6 分钟）** 内找到已知最优解；Gurobi 在约 10.7 小时内未能达到该解，且 PIM 的多数线程均找到最优解。
  - 达到相同解质量的时间优势达 **2～3 个数量级**。
- **GPU 加速有效**：
  - GPU-PIM 迭代速度比 CPU-PIM 快约 39%（时间 61%），且解质量略有提升（APD 更低）。
  - GPU-CUPIM 虽然速度更快（仅 12% 时间），但解质量下降，仍优于 Gurobi（88% 问题找到最优解）。
- **超越专用 QAP 算法**：在表 1 的 31 个实例中，CPU-PIM 和 GPU-PIM 的平均 APD（0.031%、0.028%）和运行时间（2.4 min、1.8 min）均优于 CP TS、PHA、PABC-QAP、BLS 等算法。
- **结论**：p-dit 概率伊辛机提供了一种高效、可并行、无问题特定参数的方法，在广泛 QAP 实例上优于现有求解器和专用算法。

## 7. 优点

- **建模自然**：p-dit 直接对应位置，状态对应设施，无需额外约束项，避免非法分配。
- **收敛稳健**：并行回火 + 强制交换机制有效跳出局部最优。
- **自动参数选择**：\(\beta\) 范围可通过短扫描自适应确定，减少人工调参。
- **并行友好**：算法天然适合多线程和 GPU 加速，GPU 实现显著提速。
- **通用性强**：不依赖 QAP 的特定结构（除 tai256c 特殊对称性外），可扩展至其他置换型组合优化问题。
- **实验充分性**：覆盖了标准库中几乎所有规模，并与多种方法对比，结果有说服力。

## 8. 不足与局限

- **单次运行**：每个实例仅运行一次，虽然有多线程，但未报告多次独立实验的方差（如最优解发现概率）。
- **GPU 对比选择性**：图 4 只展示了部分问题（14 个中等规模），未覆盖全部 127 个实例；GPU-CUPIM 的 APD 虽然高，但仅比 CPU-PIM 略差，可能仍有统计学意义未充分展示。
- **硬件差异**：与文献专用算法的比较（表 1）未在同一硬件上运行，时间对比不够严谨；且这些算法可能经过特别调参，而 PIM 无需调参。
- **问题规模限制**：最大测试为 n=256（tai256c），未测试更大实例（如 n=500 以上），实际工业问题可能更大。
- **GPU-CUPIM 的性能缺陷**：并发更新破坏了原始概率动力学，虽然速度提升但解质量下降，实际应用需权衡。
- **特殊问题处理**：tai256c 的特殊对称性被利用简化，论文未说明该技巧的通用性，可能无法直接推广。
- **未涉及能耗比较**：未报 GPU 与 CPU 的能耗或能效比，而这是实际部署的重要考量。

（完）
